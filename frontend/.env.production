# Production Environment for GitHub Pages
# If you run a tunnel on your laptop (Cloudflare Tunnel or localtunnel),
# set its public URL here or in GitHub repository secrets (VITE_API_BASE_URL).
# If left empty, API calls will gracefully use relative endpoints or cached content.
VITE_API_BASE_URL=
VITE_BASE_PATH=./
